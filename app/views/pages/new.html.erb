<%= link_to('<< Back to List', pages_path, :class => 'back-link') %>

<div class="page new">
    <h2>Add new page</h2>

    <%= form_for(@page) do |f| %>

        <%= render(:partial => 'form', :locals => {:f => f}) %>

        <%= f.submit('Create page') %>
    <% end %>
</div>
