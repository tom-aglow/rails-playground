<%= link_to('<< Back to List', pages_path(subject_id: @subject.id), :class => 'back-link') %>

<div class="page edit">
    <h2>Edit page</h2>

    <%= form_for(@page, url: page_path(@page, subject_id: @subject.id)) do |f| %>

        <%= render(:partial => 'form', :locals => {:f => f}) %>

        <%= f.submit('Update page') %>
    <% end %>
</div>
