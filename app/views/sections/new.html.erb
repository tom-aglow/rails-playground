<%= link_to('<< Back to List', sections_path, :class => 'back-link') %>

<div class="section new">
    <h2>New section</h2>

    <%= form_for(@section) do |f| %>

        <%= render(:partial => 'form', :locals => {:f => f}) %>

        <%= f.submit('New section') %>
    <% end %>
</div>
