<%= link_to('<< Back to List', sections_path, :class => 'back-link') %>

<div class="section edit">
    <h2>Edit section</h2>

    <%= form_for(@section) do |f| %>

        <%= render(:partial => 'form', :locals => {:f => f}) %>

        <%= f.submit('Update section') %>
    <% end %>
</div>
