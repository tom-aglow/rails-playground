<%= error_messages_for(f.object) %>

<label for="subject_name">Name</label>
<%= f.text_field(:name) %>

<label for="subject_position">Position</label>
<%= f.select(:position, 1..@subject_count) %>

<label for="subject_visible">Visible</label>
<%= f.check_box(:visible) %>

<label for="subject_visible">Created</label>
<%= f.datetime_select(:created_at, :order => [:month, :day, :year]) %>